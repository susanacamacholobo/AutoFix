import { FiltrarTipoPipe } from './filtrar-tipo-pipe';

describe('FiltrarTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
